{% extends 'base_admin.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
<style>
 body {
    background-color: #aec6cf; 
        }
 .table {
        margin-top: 20px;
        width: 100%;
        border-collapse: collapse;
    }
    .table th,
    .table td {
        border: 1px solid #ccc;
        padding: 8px;
        text-align: left;
    }
    .table th {
        background-color: #f2f2f2;
    }
</style>
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_index') }}"  class="btn btn-light">back to list</a>

    <a href="{{ path('app_category_edit', {'id': category.id}) }}"class="btn btn-warning">edit</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}

